import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10
    },
    label: {
        fontSize: 18,
        fontWeight: "bold",
        marginBottom: 5
    },
    textInput: {
        fontSize: 25,
        padding: 10,
        borderWidth: 2,
        borderColor: "#BDC3C7"
    }
})

export default styles;
