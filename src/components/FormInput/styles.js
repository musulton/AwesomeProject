import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10
    },
    label: {
        fontSize: 16,
        fontWeight: "bold"
    },
    textInput: {
        height: 50,
        fontSize: 25,
        padding: 10,
        borderWidth: 2,
        borderColor: "#BDC3C7"
    }
})

export default styles;
